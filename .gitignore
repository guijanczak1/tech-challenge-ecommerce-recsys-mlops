# Enunciado / entrada — NUNCA versionar (pode conter material da FIAP)
input/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.eggs/
*.egg-info/
build/
dist/

# Ferramentas / cache
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ML / dados / artefatos
mlruns/
mlartifacts/
mlflow.db
.dvcstore/
.dvc/cache/
data/raw/
data/processed/
models/*.pkl
models/*.pt
models/*.joblib

# Ambiente / segredos
.env
.env.*
!.env.example

# Terraform (deploy/) — binários e estado local; o lock file É versionado.
deploy/.terraform/
deploy/*.tfstate
deploy/*.tfstate.*
deploy/.terraform.tfstate.lock.info

# SO / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
