# Copie para .env (gitignored) e preencha. Lido por src/recsys/config.py.

# Caminho do arquivo YAML de hiperparâmetros.
RECSYS_PARAMS_FILE=configs/params.yaml

# MLflow (Etapa 3+). Backend SQLite local por padrão.
MLFLOW_TRACKING_URI=sqlite:///mlflow.db

# AWS (Etapa 4, bônus deploy — NÃO preencher agora; modo híbrido).
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
